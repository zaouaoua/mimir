# Deployed environment for docker-compose
# Copy to .env on the VM (same folder as docker-compose.yml)

JWT_SECRET=CHANGE_ME
PUBLIC_BASE_URL=http://localhost

SMTP_REQUIRED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Luoja back <no-reply@example.com>
